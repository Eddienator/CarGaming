# Settings includes constants and important imports, so you only have to import the settings file.

import pygame, sys, time
from random import randint, choice
from pygame import Vector2 as Vector
from os.path import join

SCREEN_WIDTH = 600
SCREEN_HEIGHT = 600
FRAMERATE = 144